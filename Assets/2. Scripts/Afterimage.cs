using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Afterimage
{

    //public Afterimage(Sprite sprite, Color color, int totalNum, float interval)
    //{
    //    MonoBehaviour.StartCoroutine(AfterimageCor(sprite, color, totalNum, interval));
    //}

    //private IEnumerator AfterimageCor(Sprite sprite, Color color, int totalNum, float interval)
    //{

    //}
}
